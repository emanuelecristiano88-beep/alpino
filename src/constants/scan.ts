/** Set in sessionStorage dopo upload scanner con paio completo (SX+DX). */
export const PAIR_STORAGE_KEY = "neuma-pair-complete";

/** JSON `ScanMetricsPayload` — metriche biometriche da ultima scansione riuscita. */
export const SCAN_METRICS_STORAGE_KEY = "neuma-scan-metrics";
