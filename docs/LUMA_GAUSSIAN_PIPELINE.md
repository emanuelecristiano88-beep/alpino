# Anteprima prodotto 3D → stampa Bambu A1

Il viewer “Luma-style 360°” è stato **rimosso** dall’app (non più esposto in Library).

Per anteprima interattiva usare **Prova virtuale (AR)** (`VirtualTryOnViewer`): modello STL/GLB + camera o fallback 3D HD.

---

**Nota tecnica (Gaussian splatting / NeRF)**: rappresentazione a “nuvola” di primitive che richiede viewer dedicato (non è un GLB triangolato classico). Se in futuro avrete export `.splat` / `.ply`, si potrà valutare un viewer dedicato separato dalla pipeline produzione.
